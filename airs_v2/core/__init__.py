"""
airs_v2.core — Canonical contracts for the v2 system.

Planned modules (Stage 1):
    state.py       — GraphState v2: typed, versioned, backwards-compatible wrapper
    schemas.py     — Pydantic v2 models (IncidentEvent, Evidence, HypothesisSet, …)
    settings.py    — Extended Settings with new LLM/embedding/vector-DB knobs
    errors.py      — Typed exception hierarchy used across all sub-packages
"""
# Stage 0: intentionally empty — scaffold only
